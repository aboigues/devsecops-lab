# Configuration ZAP baseline : une règle par ligne, « <id>	<IGNORE|INFO|WARN|FAIL>	(<nom>) » (tabulations).
# Sans ligne pour une règle, ZAP applique son niveau par défaut (WARN) : toute alerte fait échouer la gate.
#
# TODO 2 (seulement si nécessaire) : une exception = une règle précise, rétrogradée en INFO (jamais
# IGNORE), précédée d'un commentaire qui justifie la décision. Voir la règle 10049 dans .zap/baseline.conf
# à la racine du dépôt.
